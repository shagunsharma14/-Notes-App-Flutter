name: notes_app
description: A new Flutter project.
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter

  # UI
  canton_design_system:
    git:
      url: https://github.com/31Carlton7/canton_design_system.git
      ref: master
  cupertino_icons: ^1.0.2

  # Storage
  hive: ^2.0.4
  hive_flutter: ^1.1.0
  shared_preferences:

  # Other
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter
    
flutter:
  uses-material-design: true
